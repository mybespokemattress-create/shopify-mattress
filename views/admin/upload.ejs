<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Mattress Order Processor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/admin">
                <i class="fas fa-bed me-2"></i>Mattress Order Processor
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/admin"><i class="fas fa-home me-1"></i>Dashboard</a>
                <a class="nav-link" href="/admin/mappings"><i class="fas fa-link me-1"></i>Mappings</a>
                <a class="nav-link" href="/admin/stores"><i class="fas fa-store me-1"></i>Stores</a>
                <a class="nav-link" href="/admin/orders"><i class="fas fa-shopping-cart me-1"></i>Orders</a>
                <a class="nav-link active" href="/admin/upload"><i class="fas fa-upload me-1"></i>Upload</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <h1><i class="fas fa-upload me-2"></i><%= title %></h1>
                <p class="text-muted">Import multiple product mappings from a CSV file</p>
            </div>
        </div>

        <div class="row">
            <div class="col-md-8">
                <!-- Upload Form -->
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Upload CSV File</h5>
                    </div>
                    <div class="card-body">
                        <% if (typeof error !== 'undefined' && error) { %>
                            <div class="alert alert-danger">
                                <i class="fas fa-exclamation-triangle me-2"></i><%= error %>
                                <% if (typeof errors !== 'undefined' && errors && errors.length > 0) { %>
                                    <ul class="mt-2 mb-0">
                                        <% errors.forEach(err => { %>
                                            <li><%= err %></li>
                                        <% }); %>
                                    </ul>
                                <% } %>
                            </div>
                        <% } %>

                        <% if (typeof success !== 'undefined' && success) { %>
                            <div class="alert alert-success">
                                <i class="fas fa-check-circle me-2"></i><%= success %>
                                <% if (typeof stats !== 'undefined' && stats) { %>
                                    <div class="mt-2">
                                        <strong>Import Summary:</strong><br>
                                        Total rows: <%= stats.total %><br>
                                        Successful: <%= stats.successful %><br>
                                        Failed: <%= stats.failed %>
                                    </div>
                                <% } %>
                            </div>
                        <% } %>

                        <form method="POST" enctype="multipart/form-data" action="/admin/upload">
                            <div class="mb-3">
                                <label for="csvfile" class="form-label">
                                    <strong>Select CSV File</strong>
                                </label>
                                <input type="file" 
                                       class="form-control" 
                                       id="csvfile" 
                                       name="csvfile" 
                                       accept=".csv"
                                       required>
                                <div class="form-text">
                                    Upload a CSV file with your product mappings. Maximum file size: 10MB
                                </div>
                            </div>

                            <div class="row">
                                <div class="col">
                                    <button type="submit" class="btn btn-success">
                                        <i class="fas fa-upload me-2"></i>Upload and Import
                                    </button>
                                    <a href="/admin/download-template" class="btn btn-outline-info ms-2">
                                        <i class="fas fa-download me-2"></i>Download Template
                                    </a>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- Results -->
                <% if (typeof stats !== 'undefined' && stats && (stats.successful > 0 || stats.failed > 0)) { %>
                    <div class="card mt-4">
                        <div class="card-header">
                            <h5 class="mb-0">Import Results</h5>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-4">
                                    <div class="text-center">
                                        <h3 class="text-primary"><%= stats.total %></h3>
                                        <p class="text-muted">Total Rows</p>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="text-center">
                                        <h3 class="text-success"><%= stats.successful %></h3>
                                        <p class="text-muted">Successful</p>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="text-center">
                                        <h3 class="text-danger"><%= stats.failed %></h3>
                                        <p class="text-muted">Failed</p>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="text-center">
                                <a href="/admin/mappings" class="btn btn-primary">
                                    <i class="fas fa-list me-2"></i>View All Mappings
                                </a>
                            </div>
                        </div>
                    </div>
                <% } %>
            </div>

            <div class="col-md-4">
                <!-- Instructions -->
                <div class="card">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="fas fa-info-circle me-2"></i>CSV Format Instructions</h6>
                    </div>
                    <div class="card-body">
                        <h6>Required Columns:</h6>
                        <ul>
                            <li><strong>shopify_sku</strong> - Product SKU from Shopify</li>
                            <li><strong>supplier_specification</strong> - Complete supplier specification</li>
                        </ul>
                        
                        <h6>Optional Columns:</h6>
                        <ul>
                            <li><strong>shape_id</strong> - Shape identifier</li>
                            <li><strong>applicable_stores</strong> - Store restrictions (default: all)</li>
                        </ul>

                        <h6>Example Row:</h6>
                        <code class="small">
                            NOVOD272,"4inch Blue Base + White Middle","shape_boat_58","all"
                        </code>

                        <div class="mt-3">
                            <a href="/admin/download-template" class="btn btn-sm btn-outline-primary w-100">
                                <i class="fas fa-download me-2"></i>Download CSV Template
                            </a>
                        </div>
                    </div>
                </div>

                <!-- Tips -->
                <div class="card mt-3">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="fas fa-lightbulb me-2"></i>Upload Tips</h6>
                    </div>
                    <div class="card-body">
                        <ul class="small mb-0">
                            <li>Use proper CSV formatting with commas</li>
                            <li>Enclose text with commas in quotes</li>
                            <li>Ensure all required fields are present</li>
                            <li>Test with a small file first</li>
                            <li>Existing SKUs will be updated</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
